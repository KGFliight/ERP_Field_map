# Content server base URL (where manifest.json, basemaps, and layers are hosted)
# This URL hosts the manifest.json, PMTiles basemap, and KML overlay files
# Example: https://content.erp.fliight.com.au or an S3/CloudFront URL
VITE_CONTENT_BASE_URL=https://content.erp.fliight.com.au

# Optional: Fallback online satellite tile URL (used when offline basemap not downloaded)
# This should be a properly licensed tile service, NOT a consumer map provider
# Example: https://api.maptiler.com/tiles/satellite/{z}/{x}/{y}.jpg?key=YOUR_KEY
VITE_ONLINE_SATELLITE_URL=

# Optional: MapTiler API key for fallback satellite tiles (if using MapTiler)
VITE_MAPTILER_KEY=
